Deploy a fizzbuzz lambda and role

modules/iam_role will create a generic iam role
modules/lambda will create a generic lambda

main.tf uses this
variables.tf exists incase this is to be invokable (is that even a word?)
